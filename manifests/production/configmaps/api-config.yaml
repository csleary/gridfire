apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: gridfire-prod
data:
  DISABLED_PROVIDERS: "1rpc, drpc, quicknode"
  INPUT_QUEUES: client
  NODE_ENV: production
  QUEUE_TRANSCODE: transcode
  TEMP_PATH: /home/node/app/tmp
